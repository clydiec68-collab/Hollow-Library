# Copy to .env.local and fill in.

# Your live domain — used for sitemap.xml, robots.txt, Open Graph
NEXT_PUBLIC_SITE_URL=https://clydiecronje.com

# Newsletter provider key — for the /api/subscribe route when you build it
# MAILERLITE_API_KEY=
